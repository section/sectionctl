name: "🛠 Service Token: Test Docker Build"

on:
  pull_request:
    branches: [ main ]

jobs:
  docker:
    name: Run docker build
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
      - name: Run docker build
        run: |
          cd ${GITHUB_WORKSPACE}
          make all
